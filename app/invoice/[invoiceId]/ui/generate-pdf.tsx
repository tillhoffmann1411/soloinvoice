import { Button } from '@/components/ui/button';

export default function GeneratePdf() {
    return (
        <div>
            <Button
                type="submit"
            >
                Generate PDF
            </Button>
        </div>
    )
}
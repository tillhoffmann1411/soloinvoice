import { Button } from '@/components/ui/button';

export default function GeneratePdf() {
    return (
        <div>
            <Button
                variant="outline"
                type="submit"
            >
                Generate PDF
            </Button>
        </div>
    )
}